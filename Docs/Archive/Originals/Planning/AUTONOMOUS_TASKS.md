# Ночная автономная работа (2026-06-25, пока юзер спит)

Правило: только **низкорисковое и обратимое**. Любой ассет перед правкой — бэкап-дубль.
Не коммитить (юзер не просил). Не закрывать редактор без нужды (правки C++ — через Live Coding).
Монстра не трогать. Пиратские ассеты не коммитить.

---

## A. Делаю САМ ночью (автономно, проверяю своими скринами/кодом)

- [ ] **A1. Свешенная голова в превью** — диагностика + фикс центра аддитива `AO_Head_Aim`
  (центр при AimPitch=0 должен быть нейтраль). Бэкап `AO_Head_Aim_bak`. Самопроверка: скрин превью ABP.
  Если не выходит чисто — откатываю на бэкап, оставляю как было (не хуже).
- [ ] **A2. Ретаргет недостающих клипов под стейт-машину** (чтобы утром собирать сразу, без ожидания):
  - прыжок: Jump_Start / Jump_Apex(InAir) / Jump_Land (или Fall loop)
  - idle-break: дыхание/переминание (Idle fidget)
  - старт/стоп: Start_F / Stop_F (плант-фут)
  - поворот на месте: TurnInPlace L/R 90
  Складываю в `Content/Avariika/Anim/Locomotion/Mobility/` рядом с RT_MOB1_*.
- [ ] **A3. Код-ревизия последних C++-правок** (на баги, без него):
  - спринт-гейт `RefreshMoveSpeed` (forward-only dot>0.64)
  - звук шагов (порог bRun)
  - кастомизация: `WorkerAppearanceComponent` (GetSlotSearch/AvScanSlot/SetSlotByKey),
    `AvaryoHUD::DrawCustomize` + cc_* в NotifyHitBoxClick, `AvaryoPlayerController::ToggleCustomize`
  Найденное — правлю, компилю Live Coding (редактор открыт).
- [ ] **A4. Целостность BS_Loco_Mobility** — проверить задне-диагонали (Walk_BR/BL = BkPd loops),
  «залипание ног», корректность 19 семплов и сглаживания.
- [ ] **A5. WORKLOG + build-листы** — точные step-by-step под утро (стейт-машина/crouch/jump/turn-in-place),
  чтобы утром только перетаскивать ноды и подтверждать.

## B. Утром ВМЕСТЕ (нужны твои руки в графе / твои глаза / твоё решение)

- [ ] **B1. turn-in-place** — отвязка корпуса (`bUseControllerRotationYaw=false` в BeginPlay) +
  доворот тела по порогу |AimYaw|; сюда же оживёт голова-аим вбок. (меняет модель движения — нужен ты)
- [ ] **B2. Стейт-машина локомоции** — Idle↔Move, Jump, Crouch, Start/Stop, Turn-in-place
  (перетаскивание анимнод — твои руки, подключения — мои).
- [ ] **B3. Crouch-переключение** (BS_CrouchWalk_Mobility уже собран) — Blend Poses by bool.
- [ ] **B4. Idle-break** в граф (готовые клипы из A2).
- [ ] **B5. Приёмка кастомизации** — визуально проверить экран AvCustomize, повесить кнопку/клавишу,
    активировать модульное тело игрока, цвета через материалы.

## Журнал ночи (заполняю по ходу)

- (старт) план зафиксирован.
- **A1 (свешенная голова): диагностировано, фикс отложен.** `AO_Head_Aim` = 9 аддитив-семплов
  (`AAT_ROTATION_OFFSET_MESH_SPACE`, центр `Look_Center`). Свешенная голова видна ТОЛЬКО в превью-маннекене;
  в игре (2 работяги в уровне) поза чистая, руки вдоль тела. Корень — центр аддитива чуть не нулевой.
  Вслепую ночью не пересобираю (риск сломать рабочий аим) → доделаю с тобой в Фазе B1 (turn-in-place),
  там всё равно пересборка head-only поз. **РУКИ В ИГРЕ В ПОРЯДКЕ** (маска `neck_01` уже стояла, аим к рукам не лезет).
- **A2 (ретаргет клипов): НЕ НУЖЕН — всё уже есть.** 219 клипов RT_MOB1 отретаргечено: idle+4 fgt+4 conv,
  turn-in-place L/R, старты (to_Walk/Jog/Run × все углы), остановки (×plant LU/RU), прыжки (стоя+из движения),
  полный crouch-набор, смерти. Карта клипов → `LOCO_BUILD_SHEET.md`.
- **A3 (ревизия C++): сделана, крашей нет.** Находки:
  1) ~~скорости 500/750~~ **ЛОЖНАЯ ТРЕВОГА** — BP CDO = 225/540 (проверено), всё верно;
     конструктор C++ (500/750) переопределяется Blueprint'ом. Урок: читать CDO, не только конструктор.
  2) кастомизация на MP-клиенте без Server RPC не применится (соло — ок);
  3) нет скролла длинных списков опций + нет клавиши открытия (только AvCustomize).
  Я ничего не правил — только читал.
- **A4 (BS_Loco_Mobility): здоров.** 19 семплов, полный 8-way (idle@0, walk@150, jog@450),
  задне-диагонали = BkPd-петли (корректно), сглаживание 6.0. Залипания в данных нет.
  Единственное — speed-ось упирается в 450, см. находку A3#1.
- **A5: `LOCO_BUILD_SHEET.md` создан** (фазовый план сборки SM + карта клипов + находки).

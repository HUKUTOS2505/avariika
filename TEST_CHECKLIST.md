# Что протестить (сессия 2026-06-13)

Всё проверяется **в одиночку в PIE** (Play In Editor). Параметры механик вынесены в `EditAnywhere` — крутятся в редакторе без перекомпиляции. Кооп-механик в этой пачке нет (отложены).

## Дев-консольные команды (клавиша `~`, в PIE)
| Команда | Что делает |
|---|---|
| `AvGod` | вкл/выкл бессмертие (урон не проходит) — чтобы НЕ УМЕРЕТЬ во время тестов; доливает HP |
| `AvGive coffee` | выдать предмет в слот (coffee/heal/calm/ext/radio/recharge/trap/light/bio). Кофе с зарядами → пей дважды для «Кофеман» |
| `AvToolQ 3` | качество предмета В РУКАХ (0.25..3): 3 = чинить легко, 0.3 = тяжело. Держи инструмент → введи → запусти мини-игру |
| `AvMoney 50000` | выдать ₽ в кассу (для теста магазина/`AvUpgrade`/квоты) |
| `AvShop` | открыть/закрыть экран магазина «СНАРЯЖЕНИЕ» |
| `AvUpgrade Welder` | купить апгрейд за ₽ (Welder/Tester/Flashlight/Extinguisher/Radio) |
| `AvSetGear Welder 1` | задать уровень апгрейда напрямую без денег (сравнить до/после) |
| `AvBattery 2` | задать заряд фонаря 0..100 (быстро посадить → тест испуга) |
| `AvVital panic 100` | выставить шкалу (panic/health/stamina/bladder/smell, 0..100) |
| `AvIncident` | мгновенный санитарный инцидент |
| `AvGiveBio` | дать биоснаряд в руки |
| `AvCheapGear` | дешёвый комплект (моргающий фонарь) |
| `AvFinish win` / `AvFinish lose` | завершить забег, показать «Акт» |
| `AvQuota 8000` / `AvQuota 0` | включить/выключить квоту диспетчера |

---

## Чеклист механик

### 1. Канистра — полоска заправки.Работает
Почини генератор до этапа «Залить бензин» → должна **заполняться полоска** (AutoFill), как у кабеля, а не мгновенная вставка.

### 2. Новые звания «Акта».Работает но кнопку метания я бы переделал, 2 кнопки не удобно, g нажали бросила, если зажать  идет бросок, условно еобычный клик так же бросает, если больше 0.5 сек, то идет зарядка, наверно до 1.5 секунды и тогда можно бросать предмет
Вызови события и заверши забег (`AvFinish win`):
- взорвал газ → **«Пожарный наоборот»**; замкнул щиток (серия промахов) → **«Электрик Эдисон»**; покидался предметами → **«Метатель снарядов»**; хлебал кофе ≥2 раз → **«Кофеман смены»**.

### 3. Магазин — персист прогрессии (САМОЕ ВАЖНОЕ проверить)
`AvFinish win` → запомни «Касса конторы». **Закрой PIE, перезапусти, снова Play** → баланс/смена/репутация должны **сохраниться** (раньше обнулялись). Файл сейва: `Saved/SaveGames/AvariikaCompany.sav`.

### 4. Квота диспетчера.Работает
`AvQuota 8000` → на «Акте» строка **«Квота: X / 8000 ₽, срок — смена №N»**. Играй смены: набрал к сроку → квота растёт ×1.6; не набрал → **«КВОТА ПРОВАЛЕНА — контора закрыта»**, `[R]` начинает карьеру заново (баланс в ноль). `AvQuota 0` — выключить (песочница).

### 5. Кофе — диуретик.Работает
Хлебни кофе/термос → стамина вверх, паника −5, **шкала «Туалет» +18**. Несколько раз → погонит в туалет (риск инцидента). _Тюнинг: AddBladder(18) в AvaryoCharacter.cpp, Drink._

### 6. Облегчение при починке.Работает но поаника не влияет на залив бензина,и заведение генератора, другеи предметы не проверял, возможно чем больше паника тем дольше должны куриться сигареты аптечка и тд ведь страно
`AvVital panic 80` → почини любой объект → паника падает ~8 у бригады рядом (≤15 м). _Тюнинг: ReducePanic(8) в ARunState::OnObjectiveRepaired._

### 7. Облегчение после туалета.Работает
`AvVital bladder 90` → сходи в биотуалет (мини-игра) → паника −12. _Тюнинг: ReducePanic(12) в AToilet::FinishSession._

### 8. Икота при полном пузыре. ПЕРЕПРОВЕРИТЬ — шумомер теперь держит пик
`AvVital bladder 80` → каждые ~3.5 с тихий «ик» (всплеск на **шумомере** + крошечная паника). _Раньше блип гас за 0.8 с и его было не поймать; теперь пик «своего шума» держится ~0.45 с на полную, потом плавно спадает ~1.4 с — короткий «ик» (0.3) теперь видно. Тюнинг: HiccupBladderThreshold=70, HiccupInterval=3.5 (Vitals); SelfNoiseHoldTime/SelfNoiseDecayTime (персонаж)._

### 9. Отдышка после спринта. ПЕРЕПРОВЕРИТЬ — шумомер теперь держит пик
Беги, пока выносливость не упадёт в 0 → пару секунд **шумного дыхания** (шумомер дёргается, пик 0.7 каждые 0.6 с). _Тюнинг: WindedDuration=3.5 (Vitals); пик-холд шумомера — SelfNoiseHoldTime/SelfNoiseDecayTime._

### 10. Споткнуться о брошенный хлам.Работает
Урони предмет (канистра/ключ), разбегись и влети в него по курсу → **спотыкание**. _Тюнинг: конус/радиус в AvaryoCharacter::UpdateTrip (≤100 см, Dot>0.25)._

### 11. Качество инструмента → мини-игра.Работает
Выстави у инструмента `ToolQualityScale` >1 (напр. 1.6) в его BP/инстансе → его мини-игра **легче** (шире зелёная зона, медленнее курсор, шире окно стартера, мягче ритм вентиля). <1 — тяжелее, 1 — как раньше. ⚠️ Инструмент должен быть **в руках** во время мини-игры (иначе качество=1).

### 11c. Экран магазина (НОВОЕ, Фаза 2)
`AvShop` → открывается **модальный экран «СНАРЯЖЕНИЕ»**: касса конторы + 5 строк апгрейдов (Сварочник/Тестер/Фонарь/Огнетушитель/Рация) с уровнем и ценой; зелёным — по карману, красным — не хватает, серым — макс. Покупка из консоли `AvUpgrade <название>` (UI-кнопок пока нет — это след. шаг). `AvShop` ещё раз — закрыть.

### 11b. Одиночная петля прогрессии магазина (НОВОЕ).Не понял работает или нет надо добавить команду уменьшения  апгрейда что бы я сравнил
Заработай ₽ (`AvFinish win`) → купи апгрейд: **`AvUpgrade Welder`** (или Tester/Flashlight/Extinguisher/Radio) — спишет цену (3000×уровень). Теперь соответствующая мини-игра **легче** (сварка после апгрейда сварочника, щиток после Tester): зелёная зона шире, курсор медленнее. **Персистит** между сессиями (через сейв конторы). Не хватает денег / максимум — покупка не пройдёт. _Связь: тег этапа ('Welder') / тип мини-игры (Cursor→Tester) → уровень из леджера → ×качество. Прим.: для удалённого клиента в коопе уровни ещё не реплицируются (HUD-зона у не-хоста не покажет бонус) — поправлю при кооп-этапе._

### 11a. Зона мини-игр = WYSIWYG (фикс ревью).Да чем меньше паника так и есть
Зелёная зона/окно стартера **на экране теперь точно совпадают** с тем, что принимает сервер. Проверь при панике: `AvVital panic 100` → зелёная зона должна **видимо сузиться** (раньше она оставалась широкой, но сервер тайно её ужимал → необъяснимые промахи/удары током). Теперь видишь то, во что целишься. То же с качеством инструмента.

### 12. Испуг при севшей батарее. ИСПРАВЛЕНО — мгновенная команда
**Включи фонарь (F)**, затем `AvBattery 0` → фонарь гаснет, **скачок паники + вздох** (всплеск на шумомере, теперь с пик-холдом — видно). Без команды: посади свечением (DrainPerSecond=0.5, т.е. `AvBattery 2` + ждать ~4 с с включённым фонарём). _Прим.: испуг только если фонарь был ВКЛ (внезапная темнота); при выключенном `AvBattery 0` ничего не происходит — так и задумано. Тюнинг: DeadBatteryFright=12 (Flashlight)._

### 13. Страх темноты (существующая система, дедуп-проверка).Растет вроде норми.
Сломай щиток (питание вырублено) + выключи фонарь + отойди от света → паника **плавно растёт**. Включи фонарь / встань под прожектор / почини щиток → отпускает. Проверь, что **не растёт слишком быстро** (был дубль — исправлен, теперь один источник).

### 14. Раненый в темноте.я пока 1 же как понять как кто раненый а кто нет.у меня 50 паники и под фонарем растет
Раненый (ползёт) в темноте **больше не получает** прирост паники от темноты сверх «раненого» прироста.

### 15. Диспетчер помнит прошлые смены (НОВОЕ).Да было
Карьера копится в сейве. На старте смены диспетчер изредка ссылается на прошлое: если раньше **взрывал объекты** → «опять поджигатели, N зданий на счету»; если провалов больше побед → «контора на грани»; если ≥3 побед → «ветераны». Чтобы быстро проверить: несколько раз `AvFinish lose`/`win` (или взорви газ и закрой забег), потом начни новую смену (`[R]` на «Акте») — услышишь реплику-память в эфире.

### 16. Паника ↔ предметы/заливка (НОВОЕ, по твоим заметкам к #6)
`AvVital panic 100` → **дольше применяются предметы** (аптечка/сигарета — трясущиеся руки) и **медленнее льётся бензин** (AutoFill) / ставится кабель. `AvVital panic 0` → быстро. _Тюнинг: ItemUsePanicScale (персонаж), PanicHardenScale (ARepairable)._

### Метание (редизайн на G) — перепроверь после рестарта PIE
Тап **G** = слабый бросок; **зажать G** = полоска зарядки → отпустил = сильный бросок (до ×3). T больше не метает.

---

## КООП — как тестить (два разных сценария)

Кооп-харднинг (касса/апгрейды/качество инструмента/карьера теперь реплицируются хосту→клиенту через `ARunState`) собран **чистой сборкой при закрытом редакторе** — репликация новых полей зарегистрирована. Тестировать ДВУМЯ способами:

### A) Репликация геймплея — PIE на 2 игроков (главный тест харднинга)
В редакторе: выпадашка у кнопки **Play → Number of Players = 2**, **Net Mode = Play As Listen Server** → Play. Откроются 2 окна (Окно 1 = хост, Окно 2 = клиент).
Проверить, что **в Окне 2 (клиент)** всё совпадает с хостом:
1. **Магазин/касса:** на хосте `AvMoney 50000`, потом `AvUpgrade Welder`. В Окне 2 открой `AvShop` — касса и уровень Сварочника должны показывать **те же** значения (раньше у клиента касса была 0 / уровни пустые).
2. **Качество инструмента → мини-игра:** на хосте `AvSetGear Tester 3`. Клиент берёт Тестер, запускает мини-игру щитка — зелёная зона должна быть **шире** (бонус апгрейда виден и у клиента; раньше — нет).
3. **«Акт» (карьера):** заверши забег (`AvFinish win` на хосте) — у клиента в «Акте» строки кассы/репутации/карьеры (починки/взрывы) **не пустые**.
4. Базовый кооп (был раньше): перетаскивание раненого (E), совместный перекур, монитор оператора (Tab в зоне ГАЗели) — должны работать у обоих.

### B) Сессии/подключение — два отдельных запуска по LAN
Это тест **session-подсистемы** (NULL/LAN), не репликации. На одной машине: один инстанс — `AvHost` (создаёт сессию + уезжает на карту листен-сервером). Второй инстанс (вторая копия редактора в Standalone или собранный билд) — `AvFind`, затем `AvJoin 0` (подключиться к первой найденной сессии). `AvLeave` — выйти. _Реальный интернет-кооп (за NAT) — позже через EOS/Steam, нужен их плагин/конфиг от тебя._

---

## ЗВУК + VFX (2026-06-14) — что слушать/смотреть в PIE

Все звуки/эффекты — плейсхолдеры из бесплатных библиотек (Survival/Ghosthack/Asylum + NiagaraExamples), к релизу заменим/лицензируем (см. `ASSETS.md`). Параметры — `EditAnywhere` (крутятся в редакторе).

**Звук (Play на `Lvl_FirstPerson`):**
- **Шаги** — иди/беги/присядь: каденция и громкость меняются; ранен (ползёшь) — тишина.
- **Толчок (Q)** — глухой удар. **Подбор (E)** — клац-метал. **Фонарь (F)** — щелчок.
- **Аптечка** — перевязка; **сигарета** — зажигалка; **кофе/прочее** — общий звук использования.
- **Починка:** установка кабеля/расходника — клац; заливка/прокладка (AutoFill) — рабочий луп пока ползёт полоска; тык мини-игры (E) — металлический «тык».
- **Диспетчер** — «эфир рации» при каждой реплике сверху.
- **Взрыв газа** — бах + тряска. **Замыкание щитка** (3 промаха) — искры + звук.
- **Растяжка** — ставится (клац) и срабатывает (снап).
- **Эмбиент** — фоновый гул бойлерной. **Сердцебиение** — при панике (`AvVital panic 90`), только сам слышишь.

**VFX:**
- **Газовое облако** на сломанной трубе — теперь **ниже и меньше** (тюнинг `GasFXOffset`/`GasFXScale` на акторе). Чинишь — гаснет.
- **Взрыв** — вспышка Niagara. **Замыкание** — мелкие искры (`SparkScale`).

**Кооп (звук/VFX у всех):** в PIE-2 (Net Mode = Listen Server) — 2-й игрок теперь СЛЫШИТ/ВИДИТ: починку, установку, взрыв, искры, толчок, подбор, применение предметов, растяжку, шаги (через мультикаст/репликацию). Сердцебиение — намеренно личное.

---
_Сборка зелёная. Перед релизом: вернуть `VitalsComponent` стартовые Panic=0/Health=100 (сейчас 50/50 отладочные)._

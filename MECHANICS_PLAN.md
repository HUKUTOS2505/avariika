# План механик Avariika (из дизайн-спринта 2026-06-13)

Сгенерирован код-ориентированным воркфлоу (4 агента читали подсистемы + аудит баланса). Реализуем соло с билдами.

## 1. Баланс стресса
**Вердикт:** перебор был из-за ДВОЙНОГО учёта (мои дубли в ARunState) + нет потолка.
- ✅ **1.1/1.2 ДЕДУП — СДЕЛАНО:** удалены дубли темноты и заразной паники из ARunState; единый источник — `VitalsComponent` (dark rise через `IsLitByNearbyLight`, `FearContagion`), всё в одной клампленой `PanicDelta`.
- ⬜ **1.3** Раненый иммунен к dark-rise (`VitalsComponent.cpp:106` гейтить на `!bWounded`) — раненому и так +3/с.
- ⬜ **1.4** Общий кэп непрерывного прироста (`MaxSustainedPanicRisePerSecond≈3.5`, отдельный канал `AddPanicSustained`; мгновенные спайки НЕ трогать). Medium — обсудить/плейтест.
- ⬜ **1.5** Сглаживание цепочек спайков — dev-тумблер OFF, только по плейтесту.
- ⚠️ **1.6 ПЕРЕД РЕЛИЗОМ:** вернуть `VitalsComponent` стартовые Panic=0/Health=100 (сейчас 50/50 отладочные).

Спайки-события (взрыв +30, растяжка +25, биоснаряд +25, замыкание +15) НЕ кэпировать — это «удары», должны быть болезненны.

## 2. Топ-6 новых механик (solo-тестируемы, on by default)
1. **Командное «Ура, починили!»** — починка → живая бригада в радиусе `ReducePanic(~8)`. Хук: `ARunState::OnObjectiveRepaired`. Гасит панику в пиковый момент.
2. **Качество инструмента → мини-игра** — `APickupItem::ToolQualityScale`: хороший инструмент шире зелёная зона/медленнее курсор/прощает вентиль/шире окно стартера. Хуки: ARepairable `EffGreenHalf`, курсор Tick, `HandleValveTurn`, стартер `WinHalf`; связать с `bCheapGear`/апгрейдами магазина. **Даёт магазину реальный смысл.**
3. **Споткнуться о брошенный хлам** — влетел на бегу в лежащий предмет → spotykanie. Хук: `AAvaryoCharacter::UpdateTrip` + `TActorIterator<APickupItem>` (физика вкл, на полу, передний конус). Тюнить кулдаун/конус.
4. **Облегчение после туалета** — успешный визит → `ReducePanic(~12)`. Хук: `AToilet::FinishSession`.
5. **Икота при полном пузыре** — Bladder>70 → периодически тихий шум + крошечная паника. Хук: `VitalsComponent::TickComponent` (паттерн кашля).
6. **Отдышка после спринта** — стамина в ноль → пару секунд шумно дышит + лёгкая просадка хода. Хук: stamina-блок Vitals + `MakeNoise` (паттерн footstep).

**Следующая волна (нужен 2-PIE):** «Совместный ремонт / подсоби» (ассистент гасит панику-усложнение) — делает ядро кооперативным.

## 3. Отложить
- **Кооп-механики** (реанимация без аптечки, расходник тиммейту, двуручный груз, успокоить паникёра, носилки, оператор-пинг) — нужен 2-PIE харнесс, батчем после solo-волны.
- **Против инвариантов (OFF/опт-ин):** деградация брошенной работы (память: прогресс сохраняется), двуручный обязательный этап (ломает solo core-loop).
- **Высокий фрустрейшн (OFF/осторожно):** саботаж чужого ремонта, поскользнуться в своём инциденте, кофеиновый тремор (только ПОСЛЕ кэпа 1.4).
- **Нужны ассеты/геометрия:** подсадить тиммейта (нужна вертикаль на карте).
- Механик с монстром/ИИ нет (монстр на паузе — корректно).

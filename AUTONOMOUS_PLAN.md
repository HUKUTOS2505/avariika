# Автономные задачи (ночь 2026-06-14 → 15)

Юзер ушёл спать. Анимации — завтра вместе. Работаю над тем, что безопасно и ценно без него.
Принципы: геймплейную `Lvl_FirstPerson` не ломать; монстра НЕ трогать (на холде); всё новое — отдельными ассетами/картами; коммитить каждый рабочий кусок.

## Очередь

1. **[ГЛАВНАЯ] `L_House01` — первая карта на модуле дома.**
   Дублирую `LV_Horror_Light` из PostApocalypticHouse (готовый дом + хоррор-свет) → `/Game/Avariika/Maps/L_House01`. Добавляю PlayerStart, расставляю геймплей по полу (трейсы вниз): Электрощиток (Cursor+Tester), Газовая труба (Valve), Генератор (Starter+Welder), ExitZone (ГАЗель), Туалет, Тестер-предмет + немного лута. Конфиги — как в `Lvl_FirstPerson`. **Это ДРАФТ-расстановка** (строю вслепую) — юзер раздвинет по комнатам.

2. **Эмбиент в `L_House01`** — расставить 3D `AmbientSound` (Sonomar Hall/Corridor/Drips) по дому для атмосферы (теперь есть реальный уровень под них).

3. **Оптимизация текстур оператора** — basecolor 4K (26.9 МБ) + normal 4K (34 МБ) пересжать в 2K-исходник → меньше репо/памяти. Безопасно.

4. **Доки/WORKLOG** — записать что сделано, отчёт юзеру.

## НЕ делаю (нужен юзер / на холде)
- Анимации (AnimBP/ретаргет) — завтра, + юзер добавит бесплатный локомоушн-пак (Fab/Launcher, я не качаю).
- Бросок/толчок-обрезка анимаций — нужен глаз юзера.
- Монстр — на холде.
- Вернуть HP=100/Паника=0 — это перед релизом, не сейчас (нужны отладочные значения).

## Заметки
- `L_House01` ссылается на PostApocalypticHouse (он gitignored/локальный) — на чистом клоне будет битый, но мы в локальной разработке (весь пак локально). Коммичу .umap.
- Работаю headless-commandlet'ом (тяжёлая карта 4.8 ГБ + GUID капризничал) — редактор закрою.
